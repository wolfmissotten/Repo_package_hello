# Says hello

'hello()' inserts the word hello before a word, that should be a
character, that is inserted.

## Usage

``` r
hello(word = "world")
```

## Arguments

- word:

  Input of the type character

## Value

character.

## Examples

``` r
hello('wolf')
#> [1] "Hello wolf"
```
