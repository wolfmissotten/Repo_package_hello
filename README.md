# hello
This is a dummy package named hello, that momentarily only includes the functions `hello()` and `hello_detect_language()`.
To get started, see [Get started](https://wolfmissotten.github.io/Repo_package_hello/articles/hello.html), which is a vignette.
